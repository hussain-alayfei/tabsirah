# Railway configuration file for Python
[phases.setup]
nixPkgs = ["python311", "gcc", "g++"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "cd web_app && gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1 --threads 2"
